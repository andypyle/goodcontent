const expect = require('chai').expect;

describe('Module 1', () => {
	describe('#method1', () => {
		it('should do some stuff', () => {
			const array = [];
			expect(array).to.be.an('array');
		});
	});
});
